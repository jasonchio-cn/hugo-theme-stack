baseurl: https://blog.961110.xyz:10010 # 网站的URL
languageCode: zh-cn # 默认语言，zh-cn表示中文
theme: hugo-theme-stack # 主题
title: 积跬步行千里 # 网站标题，会显示在浏览器标签上
copyright: JasonChio # 网站版权所有者，显示在页面底部

# Theme i18n support
# Available values: ar, bn, ca, de, el, en, es, fr, hu, id, it, ja, ko, nl, pt-br, th, uk, zh-cn, zh-hk, zh-tw
DefaultContentLanguage: zh-cn # 网站内容的默认语言

# Set hasCJKLanguage to true if DefaultContentLanguage is in [zh-cn ja ko]
# This will make .Summary and .WordCount behave correctly for CJK languages.
hasCJKLanguage: true # 如果是中文、韩文、日文，设置为 true 以确保摘要和字数统计正确。

languages:
  zh-cn:
    languageName: 中文 # 中文语言配置
    title: 积跬步行千里 # 中文站点标题
    weight: 1 # 语言权重，数值越小排序越靠前，这里只有一种语言，无所谓权重
    params:
      sidebar:
        subtitle: JasonChio

services:
  # GA Tracking ID
  googleAnalytics:
    id: G-YQ9F51F77Q # Google Analytics 追踪 ID，用于网站流量统计。

pagination:
  pagerSize: 10

permalinks:
  post: /p/:slug/ # 博客文章的永久链接格式，使用文章的 slug 作为路径。
  page: /:slug/ # 页面内容的永久链接格式。

params:
  mainSections:
    - post # 主内容区域，显示文章内容。
  featuredImageField: image
  rssFullContent: true # RSS 提要是否包含全文内容。
  favicon: # 网站的favicon路径，例如`/favicon.ico`。

  footer:
    since: 2020 # 网站创建年份，通常显示在页脚。
    customText: 间歇性更新 持续性尝试更新 # 自定义页脚文本。

  dateFormat:
    published: 2006-01-02 # 发布日期格式。
    lastUpdated: 2006-01-02 15:04:00 # 最后更新日期格式。

  sidebar:
    emoji: 🍥 # 侧边栏头像上显示的 emoji。
    subtitle: test. # 侧边栏的副标题。
    avatar:
      enabled: true # 是否启用头像显示。
      local: false # 是否使用本地头像，我这里用的是远程链接，所以这里是false
      src: https://image.961110.xyz:10010/i/2025/03/avatar-自定义-1.webp

  article:
    math: true # 是否支持数学公式渲染。
    toc: true # 是否显示文章目录。
    readingTime: true # 是否显示预计阅读时间。
    license:
      enabled: true # 是否启用文章版权信息。
      default: Licensed under CC BY-NC-SA 4.0

  comments:
    enabled: true # 是否启用评论功能。
    provider: twikoo  # 选择的评论源，其他的都可以删掉

    twikoo:
      envId: https://comment.961110.xyz:10010
      lang: zh-CN

  widgets:
    homepage:
      - type: search # 首页的小部件，搜索功能。
      - type: archives # 首页的小部件，文章归档。
        params:
          limit: 5 # 显示的归档文章数量。
      - type: categories # 首页的小部件，文章分类。
        params:
          limit: 10 # 显示的分类数量。
      - type: tag-cloud # 首页的小部件，标签云。
        params:
          limit: 10 # 显示的标签数量。
    page:
      - type: toc # 页面的小部件，显示文章目录。

  opengraph:
    twitter:
      # Your Twitter username
      site: # 你的 Twitter 用户名，将在 OpenGraph 元数据中使用。

      # Available2 values: summary, summary_large_image
      card: summary_large_image # Twitter 卡片类型。可以选择 `summary` 或 `summary_large_image`，后者显示大图。

  defaultImage:
    opengraph:
      enabled: false # 是否为没有特色图片的页面启用默认 OpenGraph 图片。
      local: false # 是否使用本地图片作为 OpenGraph 图片。
      src: # 默认 OpenGraph 图片的路径。

  colorScheme:
    # Display toggle
    toggle: true  # 是否在页面上显示颜色模式切换按钮。

    # Available values: auto, light, dark
    default: auto  # 默认的颜色模式。可以选择自动切换（auto），或固定为亮色（light）或暗色（dark）。

  imageProcessing:
    cover:
      enabled: true # 是否为封面图片启用自动处理功能，例如裁剪、缩放等。
    content:
      enabled: true # 是否为内容图片启用自动处理功能。

### Custom menu
### See https://stack.jimmycai.com/config/menu
### To remove about, archive and search page menu item, remove `menu` field from their FrontMatter
menu:
  main: [] # 自定义主菜单的配置，可以在这里添加导航链接。

  social:
    - identifier: github # 社交链接的标识符，通常用于指定图标。
      name: GitHub # 链接的显示名称。
      url: https://github.com/jasonchio-cn # GitHub 个人主页的链接。
      params:
        icon: brand-github # 使用的社交图标，这里是 GitHub 图标。

    - identifier: bilibili
      name: Bilibili
      url: https://space.bilibili.com/30240401
      params:
        icon: brand-bilibili

    - identifier: email
      name: Email
      url: mailto:zyb389120217@gmail.com
      params:
        icon: email

related:
  includeNewer: true  # 是否在相关文章中包含较新的文章。
  threshold: 60 # 相关文章匹配的相似度阈值，范围是0到100。
  toLower: false  # 是否将标签和分类转换为小写。
  indices:
    - name: tags
      weight: 100

    - name: categories
      weight: 200

markup:
  goldmark:
    extensions:
      passthrough:
        enable: true
        delimiters:
          block:
            - - \[
              - \]
            - - $$
              - $$
          inline:
            - - \(
              - \)
    renderer:
      unsafe: true # 如果 Markdown 中包含 HTML 内容，设置为 true 以允许渲染这些 HTML。
  tableOfContents:
    endLevel: 4 # 目录生成时的最大标题级别。
    ordered: true # 目录项是否使用有序列表。
    startLevel: 2 # 目录生成时的起始标题级别。
  highlight:
    noClasses: false # 语法高亮时是否禁用 CSS 类名。
    codeFences: true # 是否启用代码块语法高亮。
    guessSyntax: true # 是否自动猜测代码块的语言进行语法高亮。
    lineNoStart: 1 # 代码行号的起始值。
    lineNos: true  # 是否在代码块中显示行号。
    lineNumbersInTable: true # 是否在表格样式中显示行号。
    tabWidth: 4 # 代码块中 Tab 的宽度（空格数）。
