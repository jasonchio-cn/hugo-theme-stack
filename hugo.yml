# ============================================================================
# 网站基础配置
# ============================================================================
baseurl: https://blog.961110.xyz:10010                    # 网站URL
languageCode: zh-cn                                       # 默认语言
theme:
  - github.com/CaiJimmy/hugo-theme-stack/v3               # 使用 go modules 拉取主题

title: 积跬步行千里                                        # 网站标题（浏览器标签显示）
copyright: JasonChio                                      # 页脚版权信息

# ============================================================================
# 多语言配置
# ============================================================================
DefaultContentLanguage: zh-cn                             # 内容默认语言
hasCJKLanguage: true                                      # CJK 语言优化（摘要/字数统计）

languages:
  zh-cn:
    languageName: 中文
    title: 积跬步行千里
    weight: 1

# ============================================================================
# 分析统计
# ============================================================================
services:
  analytics:
    google:
      id: G-YQ9F51F77Q                                    # Google Analytics V4

# ============================================================================
# 分页和链接配置
# ============================================================================
pagination:
  pagerSize: 10                                           # 每页显示数量

permalinks:
  post: /p/:slug/                                         # 文章永久链接格式
  page: /:slug/                                           # 页面永久链接格式

# ============================================================================
# 主题参数配置
# ============================================================================
params:
  mainSections:
    - post                                                # 主内容区域
  featuredImageField: image
  rssFullContent: true                                    # RSS 包含全文
  favicon: ''                                             # favicon 路径（留空使用默认）

  # ---------------------------------------------------
  # 页脚配置
  # ---------------------------------------------------
  footer:
    since: 2020                                           # 建站年份
    customText: 间歇性更新 持续性尝试更新                  # 自定义文本

  # ---------------------------------------------------
  # 日期格式
  # ---------------------------------------------------
  dateFormat:
    published: 2006-01-02
    lastUpdated: 2006-01-02 15:04:05

  # ---------------------------------------------------
  # 侧边栏配置
  # ---------------------------------------------------
  sidebar:
    emoji: 🍥                                              # Emoji 标识
    subtitle: JasonChio                                   # 副标题
    avatar:
      enabled: true
      local: false                                        # 使用远程图片
      src: https://image.961110.xyz:10010/i/2025/03/avatar-自定义-1.webp

  # ---------------------------------------------------
  # 文章配置
  # ---------------------------------------------------
  article:
    math: true                                            # 数学公式渲染
    toc: true                                             # 显示目录
    readingTime: true                                     # 阅读时间
    license:
      enabled: true
      default: Licensed under CC BY-NC-SA 4.0             # 默认许可协议

  # ---------------------------------------------------
  # 评论配置
  # ---------------------------------------------------
  comments:
    enabled: true
    provider: twikoo                                      # 评论服务提供商

    twikoo:
      envId: https://comment.961110.xyz:10010             # Twikoo 环境ID
      lang: zh-CN                                         # 语言

  # ---------------------------------------------------
  # 小部件配置（首页和页面）
  # ---------------------------------------------------
  widgets:
    homepage:
      - type: search                                      # 搜索
      - type: archives                                    # 归档
        params:
          limit: 5
      - type: categories                                  # 分类
        params:
          limit: 10
      - type: tag-cloud                                   # 标签云
        params:
          limit: 10
    page:
      - type: toc                                         # 页面目录

  # ---------------------------------------------------
  # OpenGraph 社交分享配置
  # ---------------------------------------------------
  opengraph:
    twitter:
      site: ''                                            # Twitter 用户名（留空表示不使用）
      card: summary_large_image

  # ---------------------------------------------------
  # 默认图片（无特色图时使用）
  # ---------------------------------------------------
  defaultImage:
    opengraph:
      enabled: false
      local: false
      src: ''

  # ---------------------------------------------------
  # 颜色主题
  # ---------------------------------------------------
  colorScheme:
    toggle: true                                          # 显示切换按钮
    default: auto                                         # auto/light/dark

  # ---------------------------------------------------
  # 图片处理
  # ---------------------------------------------------
  imageProcessing:
    cover:
      enabled: true                                       # 封面图处理
    content:
      enabled: true                                       # 内容图片处理

# ============================================================================
# 导航菜单
# ============================================================================
menu:
  main: []                                                # 主菜单（空数组移除所有）

  social:
    - identifier: github
      name: GitHub
      url: https://github.com/jasonchio-cn
      params:
        icon: brand-github

    - identifier: bilibili
      name: Bilibili
      url: https://space.bilibili.com/30240401
      params:
        icon: video                                        # 使用通用视频图标（暂无 bilibili）

    - identifier: email
      name: Email
      url: mailto:zyb389120217@gmail.com
      params:
        icon: email

# ============================================================================
# 相关文章配置
# ============================================================================
related:
  includeNewer: true
  threshold: 60
  toLower: false
  indices:
    - name: tags
      weight: 100
    - name: categories
      weight: 200

# ============================================================================
# Markdown 渲染配置
# ============================================================================
markup:
  goldmark:
    extensions:
      passthrough:
        enable: true                                      # 允许 HTML 直通
        delimiters:
          block:
            - - \[
              - \]
            - - $$
              - $$
          inline:
            - - \(
              - \)
    renderer:
      unsafe: true                                        # 允许渲染 HTML

  tableOfContents:
    endLevel: 4                                           # 目录最大级别
    ordered: true                                         # 使用有序列表
    startLevel: 2                                         # 目录起始级别

  highlight:
    noClasses: false                                      # 使用 CSS 类名
    codeFences: true                                      # 代码块高亮
    guessSyntax: true                                     # 自动猜测语言
    lineNoStart: 1                                        # 行号起始值
    lineNos: true                                         # 显示行号
    lineNumbersInTable: true                              # 表格样式行号
    tabWidth: 4                                           # Tab 宽度
